//! The schema itself: constraints, triggers, SQL functions and the flows they
//! support, exercised with SQL directly.

mod audit;
mod flows;
mod rbac;
mod sessions;
mod tokens;
mod two_factor;
mod users;
